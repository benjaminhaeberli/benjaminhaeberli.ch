watch:
  directories:
    - config
    - site
    - src
    - tests
  fileMask: "*.php"
notifications:
  passingTests: false
  failingTests: false
phpunit:
  binaryPath: "./vendor/bin/phpunit"
  arguments: "--prepend ./tests/prepend.php --configuration=./.phpunit.xml --colors=always ./tests/"
